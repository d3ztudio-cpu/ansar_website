# Sprouts FM Firestore change audit

Scope: the existing public `pages/ansar-sprouts` document and its verified-admin write path.

- Database: `(default)`, Standard edition, Firestore Native, `asia-south2`.
- New field: `radioStationImageUrl`, an optional public display URL.
- Read pattern: the existing single-document page read; no new query or index.
- Write pattern: the existing verified-admin `setDoc(..., { merge: true })` path.
- Validation: strict page field allowlist plus the existing optional web/root-relative URL validator (fewer than 1,000 characters).
- Authorization: unchanged; public read is intentional for website display, while create/update remains restricted to verified admins.

```json
{
  "score": 5,
  "summary": "The added field preserves the existing page authorization boundary and strict schema validation.",
  "findings": []
}
```

Red-team checks for this change: unauthenticated writes remain denied; non-admin authenticated writes remain denied; schema pollution is blocked by `hasOnlyAllowedFields`; wrong types, non-HTTP(S) values, and oversized URLs are rejected; create and update share the same complete-document validator; no owner, role, timestamp, counter, state-transition, PII, or subcollection behavior was introduced.
